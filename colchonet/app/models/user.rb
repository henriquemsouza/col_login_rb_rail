class User < ApplicationRecord
# has_secure_password
# ...dando error metodo para autenticar
#scope :confirmed, where('confirmed_at IS NOT NULL')
# ...
 # def self.authenticate(email, password)
  # confirmed.
  # find_by_email(email).
  # try(:authenticate, password)
 # end


end
